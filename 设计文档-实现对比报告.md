# 《Agent设计文档.md》 vs 当前项目实现 —— 对比报告

> 对比对象：`Agent设计文档.md`（设计意图） ⟷ `app/`（当前 MVP 实现）
> 结论先行：**骨架高度吻合，能力边界差距明显。** 设计的 6 个模块、4 个接口、12 个核心类、5 类错误码、4 组 Prompt 基本全部落地；
> 但设计里最强调的两块硬骨头——**多层 Token 防护**和**会话/配置管理**——目前只有"骨架占位"，没有实际实现。

> ## 📌 后续进展（本报告结论已过期）
> 本报告记录的是改造**前**的基线。按本文第六节的建议，已执行完整改造：
>
> | 本报告指出的缺口 | 现状 |
> |---|---|
> | P0-1 设计文档 9 处自相矛盾 | ✅ 已全部按「选项 A」在 `Agent设计文档.md` 定稿 |
> | P0-2 `config.py` 缺失（6 项配置） | ✅ 已建 `openapi_agent/config.py`，19 项配置全部支持环境变量覆盖 |
> | P0-3 Token 三层防护 0 实现 | ✅ 已建 `core/token_budget.py`；全局上限 / 单分片 / 单请求预校验三层齐备，含摘要与截断兜底 |
> | P1-4 增量 `base_openapi` 未接通 | ✅ `/generate` 接收基线契约，`diff` 输出 added/modified/deleted |
> | P1-5 `response_examples` 未进组装器 | ✅ 新增 `builder/schema_builder.py`，推导响应 schema 与错误码 |
> | P1-6 AI 推断只在 side report | ✅ 已写入 spec：`x-source` / `x-ai-inferred` / `x-conflict` |
> | P2-7 `generate_mode` / 版本可选 | ✅ 两个参数均已实现（strict/fast，3.0.3/3.1.0） |
> | P2-8 上传数量/大小限制、同名覆盖 | ✅ 已限制（10 个 / 20MB）并加序号避免覆盖 |
> | P2-9 真实进度上报、任务超时 | ✅ `progress` 0–100 整数 + `ASYNC_WORKER_TIMEOUT` |
> | P2-11 指令长度校验、控制字符过滤 | ✅ 已实现（1000 字符上限 + 控制字符清洗） |
> | 包结构 `app/` → `openapi_agent/` | ✅ 已按 2.2.1 完整重排 |
> | 测试 15 条 → 覆盖全部设计行为 | ✅ 已扩到 **58 条单测 + 36 项端到端 HTTP 冒烟**，全部通过 |
>
> **仍未完成**（保留在本报告中作为待办）：会话管理与迭代调整模块、任务持久化与多实例、OCR 单测、
> 以及设计 4.1 五项成功标准的评测集。详见改造后 `README.md` 的「已知限制」。

---

## 一、总览评分

| 设计维度 | 设计要点数 | 已实现 | 部分实现 | 未实现 | 落地度 |
|---|---|---|---|---|---|
| 1.2 使用场景（3 个） | 3 | 2.5 | 0.5 | 0 | ★★★★☆ |
| 1.3 功能模块（6 个） | 6 | 2 | 3 | 1 | ★★★☆☆ |
| 1.5 技术选型（5 层） | 5 | 4 | 1 | 0 | ★★★★☆ |
| 2.1 对外接口（4 个 + 错误码） | 9 | 9 | 0 | 0 | ★★★★★ |
| 2.2 类设计（12 个核心类） | 12 | 12 | 0 | 0 | ★★★★★ |
| 2.3 核心流程 | 2 | 2 | 0 | 0 | ★★★★★ |
| 2.4 Prompt 设计（4 个模板） | 4 | 4 | 0 | 0 | ★★★★★ |
| 2.5 Token 控制（6 条策略） | 6 | 0 | 0 | 6 | ☆☆☆☆☆ |
| 2.6 结果解析 | 1 | 1 | 0 | 0 | ★★★★★ |
| 2.8 配置项（6 项） | 6 | 1 | 1 | 4 | ★☆☆☆☆ |
| 4.1 成功标准（5 项） | 5 | 0 | 0 | 5（无度量） | ☆☆☆☆☆ |

**一句话**：`2.2/2.4/2.6`（结构、Prompt、主链路）已是"完成态"；`2.5/2.8`（Token 与配置）是当前最大的空洞。

---

## 二、已经很好对齐的部分（设计 ↔ 实现逐条对应）

### 2.1 目录与分层：几乎 1:1 落地

| 设计文档 2.2.1 包结构 | 实现 | 差异 |
|---|---|---|
| `main.py` | `app/main.py` | ✅ 一致，实现里多了"错误码→HTTP 状态映射"和 files 字段的 binary format 补丁 |
| `api/routes.py` | `app/api/routes.py` | ✅ 4 个端点齐全 |
| `api/schemas.py` | —— | ❌ 无。请求/响应模型直接写在 `routes.py` 的函数签名里（`Form/File`） |
| `api/exception_handler.py` | `app/main.py:63` | ⚠️ 合并进 main.py，无独立模块 |
| `core/task_manager.py` | `app/core/task_manager.py` | ✅ |
| `core/agent_workflow.py` | `app/agent/workflow.py` | ⚠️ 改成 `agent/` 包，设计里没有 `agent/` 这一层 |
| `core/llm_client.py` | `app/agent/llm_client.py` | ⚠️ 同上 |
| `core/prompt_templates.py` | `app/agent/prompts.py` + `prompts/*.txt` | ✅ 且更好：模板外置成 txt，非硬编码常量 |
| `material/preprocessor.py` | `app/preprocessing/preprocessor.py` | ⚠️ 包名 `material/` → `preprocessing/` |
| `material/file_parser.py` | `app/preprocessing/file_parser.py` | ✅ |
| `material/extractor.py` | `app/agent/extractor.py` | ⚠️ 挪到了 `agent/` |
| `model/intermediate_meta.py` | `app/agent/models.py` | ⚠️ 包名 `model/` → `agent/`，且没有 `openapi_build_model.py` |
| `builder/openapi_assembler.py` | `app/openapi_build/assembler.py` | ⚠️ 包名 `builder/` → `openapi_build/` |
| `builder/validator.py` | `app/openapi_build/validator.py` | ✅ |
| `builder/diff_helper.py` | `app/openapi_build/diff.py` | ✅ |
| `report/risk_report_builder.py` | `app/reporting/risk_report.py` | ⚠️ 包名 `report/` → `reporting/` |
| `config.py` | —— | ❌ **完全缺失**（最大缺口之一） |
| —— | `app/core/errors.py` | ➕ 设计里没写，实现新增了，且比设计的错误码方案更规整 |
| —— | `app/preprocessing/splitter.py` | ➕ 设计没写独立分片模块，实现拆出来了 |
| —— | `tests/` `scripts/` `examples/` | ➕ 设计未提，实现有 15 个用例 + 可复现脚本 |

> 结论：**包名差异属于重构自由，不影响正确性**；但 `api/schemas.py`、`model/openapi_build_model.py`、`config.py` 三个设计明确的文件确实不存在。

### 2.2 四个对外接口：完全一致

| 设计 | 实现 | 一致性 |
|---|---|---|
| `POST /api/v1/openapi/generate` | `routes.py:44` | ✅ |
| `GET /task/{task_id}/status` | `routes.py:147` | ✅ 返回 `progress` 字段（但恒为空，见下） |
| `GET /task/{task_id}/result` | `routes.py:158` | ✅ 返回 openapi/diff/risk_report |
| `POST /task/{task_id}/cancel` | `routes.py:176` | ✅ |

### 2.3 错误码：5 个全部一致

| code | 设计含义 | 实现 `ErrorCode` | HTTP | 一致 |
|---|---|---|---|---|
| 10001 | 输入参数错误 | `INPUT_ERROR` | 400 | ✅ |
| 10002 | task_id 不存在 | `TASK_NOT_FOUND` | 404 | ✅ |
| 10003 | 素材解析错误或大小异常 | `MATERIAL_ERROR` | 422 | ✅ |
| 10004 | 大模型调用失败或超时 | `LLM_ERROR` | 503 | ✅ |
| 10005 | 服务未知内部错误 | `INTERNAL_ERROR` | 500 | ✅ |

### 2.4 类设计：设计点名的 12 个类全部存在

`BusinessException` / `Task` / `TaskManager` / `AgentWorkflow` / `LlmClient` / `MaterialPreprocessor` / `FileParser` / `MetaExtractor` / `ApiIntermediateMeta` / `OpenApiAssembler` / `OpenApiValidator` / `DiffHelper` / `RiskReportBuilder`

实现额外新增（设计未覆盖但必要）：`ErrorCode`、`TaskStatus`、`RawEndpoint`、`FusedEndpoint`、`ExtractionResult`、`RiskScanResult`、`recursive_split`。

### 2.5 四个 Prompt 模板：文本级别一致

| 设计 2.4.1 | 实现文件 | 一致性 |
|---|---|---|
| 通用基础 Prompt | `prompts/base_system.txt` | ✅ 几乎逐字一致（设计里多了个错别字"能力。。"） |
| 元数据提取 Prompt | `prompts/extract.txt` | ✅ 一致，末尾补充了 JSON schema 约束（比设计更可执行） |
| 融合 & 冲突修正 Prompt | `prompts/fuse.txt` | ✅ 一致，末尾补充了 JSON schema |
| 风险识别 Prompt | `prompts/risk_scan.txt` | ✅ 一致，末尾补充了 JSON schema |

`prompts.py` 的 `render()` 实现了 2.4.2 的变量填充规则（`{{material_content}}` / `{{user_instruction}}` / `{{raw_meta_list}}` / `{{final_meta_list}}`）。
⚠️ 但 2.4.2 里写的"转义 JSON 特殊字符""过滤高危控制字符"**没有实现**。

### 2.6 主流程：设计 2.3.1 的流程图与 `workflow.py` 完全对应

```
设计: FileParser → Preprocessor → MetaExtractor → 自研合并 → Assembler → Validator → Diff → RiskReport
实现: preprocessor.process() → extractor.extract() → Assembler.assemble() → RiskReport.build() → Validator.validate() → DiffHelper.diff()
```
⚠️ 唯一顺序差异：实现把 **Validator 放最后**（且失败不抛错、写进 `risk_report.validation_errors`），设计是"校验失败抛出业务异常"。
这是实现的**有意降级**（README 明说"失败不丢弃文档"），比设计更合理，建议反向更新设计文档。

---

## 三、设计有、实现没有（真正的差距）

### 🔴 P0：Token 控制体系（设计 2.5 + 2.3.2，6 条策略，0 条实现）

这是设计文档里着墨最多、也最"像生产系统"的部分，但代码里**一行都没有**。

| 设计 2.5 策略 | 实现现状 | 证据 |
|---|---|---|
| 全局素材总 token 上限校验（`TOTAL_MATERIAL_TOKEN_LIMIT`） | ❌ 无任何 token 统计 | `preprocessor.py` 只做长度过滤 `MIN_CONTENT_LEN=20` |
| 素材分片控制单分片 token | ⚠️ 只按**字符数**切分（1500 字符），非 token | `splitter.py:1` |
| 单 LLM 请求 token 预校验 | ❌ `chat_json()` 直接发请求 | `llm_client.py:32` |
| LLM 侧返回超限异常兜底（context overflow → 业务异常） | ⚠️ 所有异常统一归到 10004，不区分"上下文超限" | `llm_client.py:53` |
| 避免元数据二次进 LLM（优先代码合并/冲突检测） | ✅ 部分做到：一致分组本地去重不发请求 | `extractor.py:78-81` |
| 上下文截断 `truncate_text_by_token` + `estimate_token` | ❌ 无此函数 | 设计 2.5 代码示例未落地 |
| 输入摘要 `summary_fragment`（类签名+方法签名压缩） | ❌ 无此函数 | 设计 2.5 代码示例未落地 |
| 2.3.2 流程图里的 "超限 → 抛 10003 任务失败" | ❌ 不存在这个分支 | —— |

**影响**：素材一大，行为不是"干净地报错 10003"也不是"摘要压缩"，而是**把超长 prompt 直接发给模型**，最后拿到一个笼统的 10004。设计里承诺的"Token 稳定性维度：超限容错率 100%"目前无法成立。

### 🔴 P0：配置层（设计 2.8.1，6 项配置，1 项落地）

| 设计配置项 | 默认值 | 实现 |
|---|---|---|
| `claude.api-key` | - | ⚠️ 有，但改叫环境变量 `OPENAPI_AGENT_API_KEY` / `BASE_URL` / `MODEL`，硬编码在 `routes.py:28-41` |
| `MODEL_MAX_CONTEXT` | 128000 | ❌ 无 |
| `TOTAL_MATERIAL_TOKEN_LIMIT` | 100000 | ❌ 无 |
| `ENABLE_CONTEXT_TRUNCATE` | False | ❌ 无 |
| `USER_INSTRUCTION_MAX_LEN` | 1000 | ❌ 无校验（`user_instruction` 长度不设限） |
| `ASYNC_WORKER_TIMEOUT` | 300 | ❌ 无任务级超时（只有 `LlmClient` 的 60s 单请求超时） |
| —— | —— | ➕ `chunk_size=1500` / `overlap=150` 硬编码在 `MaterialPreprocessor.__init__` |

**核心问题：`config.py` 不存在**。所有可调参数散落在构造函数默认值和环境变量里，无法集中控制——这直接违反设计 2.8.1 的意图。

### 🟠 P1：会话管理与迭代调整（设计 1.3 模块 6，0 实现）

设计中"【系统配置与会话管理模块】管理 Agent 参数配置，维护会话上下文，支持接口分组，支持迭代调整文档"——**完全不存在**：
- 无会话（session）概念，`TaskManager` 只有一次性任务；
- 无接口分组 API（实现是按 path 首段自动打 tag，不可配置）；
- 无"基于上次结果继续调整"的迭代入口（只能靠 `base_openapi` 重新跑，且该参数还没接通，见下）。

### 🟠 P1：增量生成链路未接通（设计场景 1）

- 设计场景 1 是"版本迭代 → 输出新 OpenAPI + 新旧 diff"；
- 实现里 `DiffHelper` ✅、`Assembler(base_spec=...)` ✅、`AgentWorkflow.run(base_spec=...)` ✅ **三层都写好了**；
- 但 `routes.py:109` 调用时硬传 `None`，且 `/generate` **不接收 `base_openapi` 参数**；
- 结果：`res.json` 里 `diff` 恒为 `null`，场景 1 实际不可用。

### 🟠 P1：响应结构组装缺失（设计 2.6.1 隐含要求 + 场景 2）

- `RawEndpoint.response_examples` 已经从素材抽出来了（`extractor.py:21`）；
- 但 `ApiIntermediateMeta` **没有 response 字段**，`assembler._operation()` 写死 `{"200": {"description": "OK"}}`；
- 结果：产物**不能直接给前端做 mock**（设计 1.2 场景 3 明确说要"可直接交给前端做 mock"），风险报告里那 11 条 `missing_info` 里也反复提示"缺少响应结构"。

### 🟡 P2：生成模式与 OpenAPI 版本（设计 1.3 模块 4 + 2.1.1）

| 设计 | 实现 |
|---|---|
| "提供严谨 / 快速两种生成模式" | ❌ 无 `generate_mode` 参数 |
| 输入示例里的 `"generate_mode": "strict"` | ❌ 该字段不被接收 |
| 用户指令 `"OpenAPI 版本使用 3.1"` | ❌ 版本硬编码 `"3.0.3"`（`assembler.py:25`），不可选 |
| "标记 AI 推测内容" | ⚠️ 只在 `risk_report.ai_infer_items` 里标，**没有落到 spec 内**（无 `x-ai-inferred` 之类的扩展字段） |

### 🟡 P2：其余零散缺口

| 设计位置 | 设计内容 | 实现 |
|---|---|---|
| 2.7 边界测试 | "上传最多 10 个文件" | ❌ 无数量限制（设计 3.1 提到"最多 10 个"） |
| 2.7 边界测试 | 文件大小限制 | ❌ 无（同名字段还会在临时目录互相覆盖） |
| 2.4.2 | 用户指令过滤高危控制字符 | ❌ 无 |
| 2.4.2 | 变量 JSON 特殊字符转义 | ❌ 无（用 `str.replace` 裸拼） |
| 2.1.1 | `task_id` 形如 `task-8a2f-4412-...` | ⚠️ 实现用 `uuid4().hex`（32 位无连字符） |
| 2.2.2 | `Task.progress: int` | ⚠️ 实现是 `progress: str = ""` 且恒为空，11 个中间状态只写了 2 个 |
| 2.2.2 | `TaskManager.create_task(task_id, input_materials)` | ⚠️ 实现是 `create_task(input_materials)`，task_id 内部生成 |
| 2.5 | "只做文本处理，不运行/编译代码" | ✅ 遵守（源码只按纯文本读） |
| 1.4 | 三条"明确不做" | ✅ 全部遵守 |
| 2.8.2 | 嵌入项目使用（`from agent.workflow import ...`） | ⚠️ 路径应为 `from app.agent.workflow import AgentWorkflow`；`LlmClient` 在 `app.agent.llm_client` 而非 `agent.client` |

### ⚪ 设计里已列但无法验证的

- 4.1 的 5 项成功标准（准确率 ≥90%、覆盖率 100%、响应 ≤10s、任务成功率 ≥90%）**没有任何度量代码或评测集**，当前只有 15 个 Fake LLM 单测（验证"流程不崩"，不验证"提取准不准"）。
- 附录 A/B/C 是空模板（`# 【填写完整模板】`、`TC001 | |`）。

---

## 四、设计文档自身的逻辑不一致（建议先修文档）

写文档时留下的错误码/字段错位，会直接误导实现者：

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| 1 | 2.2.2 `TaskManager.cancel_task` 代码骨架 | 抛的是 `code=20001, "task_id不存在"`，而 2.1.2 错误码表里"task_id不存在"是 **10002**，且 **20001 根本没在表里定义** | 改成 `10002` |
| 2 | 2.6.1 错误返回示例 | `{"code": 10003, "msg": "参数非法，user_instruction过长"}` —— 参数非法应是 **10001**，10003 是素材解析错误 | 改成 `10001` |
| 3 | 2.5 输入摘要示例 | 抛 `BusinessException(30003, ...)`，**30003 不在错误码表** | 统一为 10003 |
| 4 | 2.7 边界测试-Token 超限 | 预期"返回错误码 **10004**"（10004 是 LLM 失败）；而 2.3.2 流程图同一场景写的是"抛 **10003**" | 二选一并统一 |
| 5 | 2.7 边界测试-空输入 | 预期"错误码 **10003**"；而 2.1.2 + 实现都是空输入 = **10001** | 改成 10001 |
| 6 | 2.1.1 输入示例 | 字段名 `text_materials`；3.1 表格里又叫 **`text_materials`**，但实现和 2.8.2 是 **`text_material`(单数)** | 统一为 `text_materials` 或 `texts` |
| 7 | 2.1.1 vs 2.8.1 | 示例含 `base_openapi` / `generate_mode` / `output_format`，但 2.8.1 配置表里**列的是 `claude.api-key`**，与技术选型"兼容 OpenAI 协议 LLM"矛盾 | 统一为 OpenAI 协议 + `OPENAI_*` |
| 8 | 2.2.2 类表格 | `TaskManager` 一行把 `create_task()get_task()cancel_task()` 挤在一个单元格，参数/返回值串行，可读性差 | 拆成三行 |
| 9 | 2.2.1 包结构 | 设计 `core/` 放 workflow 和 llm_client，`material/` 放 extractor；实现却是 `agent/` 包承载 extractor+workflow+llm+models+prompts | 以实现为准更新（更内聚） |
| 10 | 2.4.1 功能1 | "接口文档标准化能力。。" 双句号 | 笔误 |
| 11 | 附录 A/B | 空占位符 `【填写完整模板】` | 要么删掉，要么补全 |
| 12 | 2.8.2 嵌入示例 | `from agent.client import LlmClient`，实际是 `app.agent.llm_client`；构造函数签名也不同（实现是 `LlmClient(base_url, api_key, model)`） | 按实现更新 |
| 13 | 2.2.2 `AgentWorkflow.run()` 返回值 | 写 `(List[ApiIntermediateMeta], dict)`，与实现一致 ✅；但参数表写"preprocessor：素材预处理实例"，设计 2.2.2 的 `MaterialPreprocessor.process()` 参数又写"【无】"，实际是构造函数接 files/texts | 补全参数说明 |

---

## 五、实现比设计更好的地方（建议反向吸收进设计文档）

1. **降级四路径**：设计是"失败即抛异常/任务失败"，实现是"产出永远给，问题全部写进 risk_report"（单块抽取失败跳过、融合失败回退首份+强制 `is_conflict`、风险 LLM 失败保留规则结果、校验失败写入 `validation_errors`）。这套比设计更符合"可人工复核"的产品目标。
2. **JSON 纠错重试**：设计 `chat_json()` 只写"捕获大模型异常"，实现把错误输出回灌给模型要求重生成（`llm_client.py:41-50`），`max_retries=2`。
3. **34 种格式 + zip 递归 + OCR**：设计只说"各类文件、压缩包、截图"，实现有明确的 `PARSABLE_EXTS` 白名单和 zip 内递归解析。
4. **`ErrorCode` IntEnum + `ERROR_HTTP_STATUS` 映射表**：比设计里散落的裸数字规范。
5. **`prompts/*.txt` 外置 + `load_prompt`/`render`**：比设计里"prompt 模板常量"更易维护。
6. **`scripts/rerun_sample.py` + `examples/sample_接口文档.md`**：可复现的产物快照 + 预埋脏数据（字段冲突、默认值不一致、待过滤的内部调试接口），这是设计里完全没有的"可验证性"资产。
7. **`FakeLlmClient`**：整套流水线无真实模型调用即可确定性测试，含异常与降级路径。

---

## 六、按优先级补齐的建议

### P0（补上才算"设计兑现"）
1. **新建 `app/config.py`**：集中 `MODEL_MAX_CONTEXT` / `TOTAL_MATERIAL_TOKEN_LIMIT` / `ENABLE_CONTEXT_TRUNCATE` / `USER_INSTRUCTION_MAX_LEN` / `ASYNC_WORKER_TIMEOUT` / `chunk_size` / `overlap`，全部支持环境变量覆盖。
2. **实现 Token 三层防护**（设计 2.5 + 2.3.2）：
   - `estimate_token(text)`（现有 `len//4` 就够 MVP）；
   - `MaterialPreprocessor.process()` 累计全素材 token，超 `TOTAL_MATERIAL_TOKEN_LIMIT` → `10003`；
   - `LlmClient.chat_json()` 调用前预校验 `prompt + 分片 + 指令` 总量；
   - 捕获上游 context-overflow 错误 → 归一为 `10003`（而非笼统 10004）；
   - 可选：`ENABLE_CONTEXT_TRUNCATE=True` 时走 `truncate_text_by_token`，否则直接失败。
3. **修文档错误码错位**（第四节 1–5 条），否则实现者会照着错表写。

### P1（补齐场景 1 / 2 / 3 的完整性）
4. **接通 `base_openapi`**：`/generate` 接收基线契约（JSON 字符串或上传），透传到 `AgentWorkflow.run(base_spec=...)`，让 `diff` 不再恒为 `null`。
5. **`response_examples` 进入组装器**：给 `ApiIntermediateMeta` 加 `responses: dict | None`，`assembler` 据此生成 200 schema + 错误码，产物才能真正做 mock。
6. **AI 推断落到 spec 内**：给 operation/schema 打 `x-ai-inferred: true` + `x-conflict: true` 扩展字段，让"不可信"信息跟着契约走，而不是只躺在 side report 里。

### P2（体验与生产化）
7. `generate_mode: strict|fast`、OpenAPI 版本可选（3.0.3 / 3.1）。
8. 上传数量（≤10）与单文件大小限制；同名文件加前缀避免覆盖。
9. 真实 `progress` 上报（11 个状态已定义，只用了 2 个）+ 任务级超时 `ASYNC_WORKER_TIMEOUT`。
10. 会话/任务持久化（SQLite）与 TTL 清理，支撑多实例。
11. `user_instruction` 长度校验（1000）+ 控制字符过滤 + prompt 变量转义。
12. 建一个 ≥20 条真实素材的评测集，才能验证 4.1 的成功标准（当前 5 项指标零度量）。

---

## 七、一句话结论

当前项目是**设计文档的"主链路 MVP 切片"**：架构、接口、Prompt、主流程、类划分全部对齐甚至超出，
但**设计的"生产级防护"部分（Token 多层控制、集中配置、超时、会话管理、增量接通、评测指标）尚未开始**。
建议路径：先修文档错误码错位 → 补 `config.py` + Token 防护（P0）→ 接通 `base_openapi` 与响应组装（P1）→ 再谈生成模式、持久化与评测（P2）。

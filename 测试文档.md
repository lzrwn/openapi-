# OpenAPI 契约智能生成 Agent —— 测试文档

| 项目 | 内容 |
|---|---|
| 被测系统 | OpenAPI 契约智能生成 Agent（`openapi_agent/`） |
| 对应设计文档 | `Agent设计文档.md` |
| 版本 | v0.1.0（MVP） |
| 测试结论 | **通过** —— 63 个单测 + 36 项端到端 HTTP 冒烟全部通过 |
| 文档维护 | 用例变更时同步更新本文件的 TC 编号与"覆盖状态"列 |

---

## 一、测试目标与范围

### 1.1 测试目标

1. 验证四个对外接口（`generate` / `status` / `result` / `cancel`）的契约、状态流转与错误码符合设计文档 2.1；
2. 验证「产出永远给，问题全部显式标注」的可靠性原则：四条降级路径在单点失败时不中断产出；
3. 验证 Token 三层防护在超限时的行为可控（拒绝或降级，绝不产出残缺文档或吞掉错误）；
4. 验证契约产物本身合法且可用：OpenAPI 规范校验通过、响应结构可 mock、中文分组与接口名正确落位；
5. 验证入参校验与错误码映射在边界输入下的确定性（不依赖模型行为）。

### 1.2 范围内

- 接入层：`api/routes.py`、`api/validators.py`、`api/exception_handler.py`、`api/schemas.py`
- 核心层：`core/agent_workflow.py`、`core/task_manager.py`、`core/llm_client.py`、`core/token_budget.py`
- 素材层：`material/file_parser.py`、`material/preprocessor.py`、`material/splitter.py`、`material/extractor.py`
- 构建层：`builder/openapi_assembler.py`、`builder/schema_builder.py`、`builder/validator.py`、`builder/diff_helper.py`
- 报告层：`report/risk_report_builder.py`

### 1.3 范围外（本版不测）

- 大模型自身的提取准确率（需评测集 + 真实模型，见第五节 G-01）
- 前端 UI（当前无 UI）
- 多实例部署、持久化、鉴权与限流（当前为进程内内存实现）

---

## 二、测试环境

### 2.1 环境要求

| 项 | 要求 |
|---|---|
| Python | >= 3.10 |
| 依赖 | `pip install -e ".[dev]"`（pytest、httpx） |
| 操作系统 | Windows / Linux / macOS 均可 |
| 模型服务 | 单测与冒烟**不需要真实模型**（使用假 LLM）；性能与准确率测试需要 |

### 2.2 两种"假模型"方案

| 方案 | 用途 | 位置 |
|---|---|---|
| 注入式 `FakeLlmClient` | 单测。按 schema 注入应答，支持异常注入、延迟、纯文本调用 | `tests/fakes.py` |
| 本地假 OpenAI 服务 | 端到端冒烟。真实 HTTP 进程 + 真实网络调用链 | `scripts/mock_llm_server.py` |

### 2.3 执行方式

```bash
# 1) 单元测试（默认）
python -m pytest -q
# 63 passed

# 2) 单文件 / 单用例
python -m pytest tests/test_token_control.py -v
python -m pytest tests/test_build.py::test_tag_and_summary_from_metadata -v

# 3) 端到端 HTTP 冒烟（需要两个进程 + 一个客户端）
python scripts/mock_llm_server.py 11434                       # 终端 1
OPENAPI_AGENT_BASE_URL=http://127.0.0.1:11434/v1 \
  OPENAPI_AGENT_API_KEY=mock \
  python -m uvicorn openapi_agent.main:app --port 8123        # 终端 2
python scripts/smoke_http.py http://127.0.0.1:8123            # 终端 3
```

> 环境变量清单见 `README.md`「3. 配置」；19 项配置全部可覆盖。

---

## 三、测试策略

### 3.1 分层

```
        ┌──────────────────────────────────────────────┐
        │ L3 端到端冒烟  36 项检查                       │  真实 uvicorn + 真实 HTTP + 假模型
        │    scripts/smoke_http.py                     │  验证跨层装配、状态流转、错误码
        ├──────────────────────────────────────────────┤
        │ L2 集成测试    tests/test_api_flow.py (3)      │  TestClient + 注入假模型
        │                tests/test_api_validation.py(14)│  验证接口契约与降级
        ├──────────────────────────────────────────────┤
        │ L1 单元测试    test_core(4) test_build(10)     │  纯函数与单类，无 IO
        │                test_token_control(15)          │
        │                test_validators(11)             │
        │                test_workflow_flow(6)           │
        └──────────────────────────────────────────────┘
```

### 3.2 关键原则

| 原则 | 说明 |
|---|---|
| **零真实模型依赖** | 全部 63 个单测与 36 项冒烟均可离线确定性运行，CI 无密钥也能跑 |
| **失败路径优先** | 每条降级路径（跳过/回退/保留规则结果/写入报告）都有独立用例，而非只测 happy path |
| **边界可注入** | 配置项通过 `Settings` 对象注入（`_settings(**overrides)`），无需改环境变量即可构造超限场景 |
| **断言落在可观测输出** | 断言 HTTP 状态码、错误码、`risk_report` 结构、契约字段，而非内部调用细节 |

### 3.3 覆盖统计

| 测试文件 | 用例数 | 覆盖模块 |
|---|---|---|
| `tests/test_core.py` | 4 | `core/errors.py`、`core/task.py`、`core/task_manager.py` |
| `tests/test_build.py` | 10 | `builder/*`、`report/*`、`material/splitter.py` |
| `tests/test_token_control.py` | 15 | `core/token_budget.py`、`core/llm_client.py`、`material/preprocessor.py` |
| `tests/test_validators.py` | 11 | `api/validators.py` |
| `tests/test_workflow_flow.py` | 6 | `core/agent_workflow.py`、`material/extractor.py` |
| `tests/test_api_flow.py` | 3 | `api/routes.py` → 全链路 |
| `tests/test_api_validation.py` | 14 | `api/routes.py` → 校验、模式、增量、状态 |
| **合计** | **63** | 收集命令：`python -m pytest --collect-only -q` |

---

## 四、测试用例

编号规则：`TC-U-*` 单元、`TC-T-*` Token 控制、`TC-V-*` 入参校验、`TC-I-*` 集成/接口。
一个用例对应一个测试函数；若该函数带参数化，运行时的 pytest 用例数会多于编号数（当前 59 个编号 → 63 条运行时用例，差额来自 4 处参数化：非法枚举 3、非法基准契约 3、指令长度边界）。

错误码对照：`10001` 输入错误(400) / `10002` 任务不存在或不可取消(404) / `10003` 素材·Token·上下文异常(422) / `10004` LLM 失败(503) / `10005` 服务内部错误(500)。

### 4.1 单元测试 —— 核心模型与任务（`test_core.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-U-01 | `test_business_exception_format` | 构造 `BusinessException(10001,…)`；带 `detail` 的异常 | `code==10001`；`__str__` 含 message；`detail` 可读 | 2.2.2 |
| TC-U-02 | `test_task_defaults` | `Task(task_id="t1")` | `status=pending`、`progress==0`(int)、`input_materials=={}`(dict)、`result/error_info is None` | 2.2.2 |
| TC-U-03 | `test_new_task_id_format` | 调 `new_task_id()` | 形如 `task-xxxxxxxx-xxxx-xxxx-xxxxxxxxxxxx`，总长 40 | 2.1.1 |
| TC-U-04 | `test_task_manager_lifecycle` | 创建 → 查询 → 取消；再对不存在/已终态任务取消 | 查询命中同一实例；取消后置 `cancelled`；两种非法取消均抛 `10002` | 2.2.2 |

### 4.2 单元测试 —— 组装、校验、报告、切块（`test_build.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-U-05 | `test_assemble_produces_valid_openapi` | 2 条元数据（POST 带请求体、GET 带路径参数） | `openapi==3.0.3`；请求体字段类型正确；规范校验通过 | 2.6 |
| TC-U-06 | `test_diff_categories` | 旧 `{a:1,b:2}` / 新 `{a:1,b:3,c:4}` | `added` 含 `c`、`modified` 含 `b`、`deleted` 为空 | 2.2.2 |
| TC-U-07 | `test_risk_report_flags` | 元数据含 `is_conflict=True` 且均无返回示例 | 冲突项命中该接口；`ai_infer_items==[]`；全局与逐接口均提示缺响应结构 | 2.4.1 功能4 |
| TC-U-08 | `test_risk_report_respects_response_examples` | 其中 1 条接口补上 `response_examples` | 仅未给示例的接口报"缺少响应结构定义"；不再输出该全局告警 | 2.4.1 功能4 |
| TC-U-09 | `test_tag_and_summary_from_metadata` | 3 条元数据，带中文 `summary`/`tag`（含"查询文件列表"） | `operation.summary`、`operation.tags` 取中文值；根节点 `tags` 按首次出现顺序声明且只含被用到的分组；路径原样 | 2.2.2 / 2.4.1 |
| TC-U-10 | `test_tag_falls_back_to_first_path_segment` | 元数据无 `tag` | 分组回落为路径首段（`/api/v1/products`→`api`、`/users`→`users`） | 2.4.1 功能3.5 |
| TC-U-11 | `test_summary_falls_back_to_method_and_path` | 元数据无 `summary` | `summary` 回落为 `GET /api/v1/products` | 2.4.1 功能3.5 |
| TC-U-12 | `test_base_spec_tags_are_inherited_and_preserved` | 基准契约已声明同名分组及说明 | 分组顺序为新接口优先；基准契约中该分组的 `description` 被继承而非覆盖 | 场景1 |
| TC-U-13 | `test_risk_report_flags_missing_summary_and_tag` | 元数据无 `summary`、无 `tag` | 逐接口提示"缺少接口功能名称（summary）"；全局提示"未识别出业务分组（tag）" | 2.4.1 功能3.5 |
| TC-U-14 | `test_recursive_split` | 20 段各 200+ 字符，`chunk_size=500` | 切出多块、每块 ≤500；空串返回 `[]` | 2.5 |

### 4.3 单元测试 —— Token 三层防护（`test_token_control.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-T-01 | `test_estimate_token` | 空串 / 4 字符 / 400 字符 | `0` / `1` / `100` | 2.5 |
| TC-T-02 | `test_truncate_text_by_token` | 4000 字符按 100 token 截断；短文不截断 | 尾部追加截断警告、长度下降；短文原样返回且标志为 `False` | 2.5 |
| TC-T-03 | `test_truncate_rejects_bad_limit` | `max_token=0` | 抛 `10003` | 2.5 |
| TC-T-04 | `test_is_context_overflow` | 三类上游报错文案 | 上下文类文案命中；`401 unauthorized` 不命中 | 2.5 |
| TC-T-05 | `test_summary_fragment_uses_chat_raw` | 超长分片 + 充足预算 | 走 `chat_raw` 返回摘要；`raw_calls` 有记录 | 2.5 |
| TC-T-06 | `test_summary_fragment_rejects_oversized_fragment` | 分片 ≈1000 token，预算 10 | 抛 `10003` 且**未发起**调用 | 2.5 |
| TC-T-07 | `test_preprocessor_rejects_total_token_over_limit` | `TOTAL_MATERIAL_TOKEN_LIMIT=50`，素材远超 | 抛 `10003`，文案含"token 上限" | 2.3.2 第1层 |
| TC-T-08 | `test_preprocessor_records_total_tokens` | 普通素材 | `total_tokens>0` 且等于各分片估算之和 | 2.5 |
| TC-T-09 | `test_preprocessor_rejects_oversized_chunk_when_truncate_disabled` | 单分片超预算且 `ENABLE_CONTEXT_TRUNCATE=False` | 抛 `10003`，文案含"分片过大" | 2.3.2 第2层 |
| TC-T-10 | `test_preprocessor_summarizes_chunk_when_truncate_enabled` | 分片 ≈2000 token > 触发阈值 1500，截断开启 | 分片内容被替换为摘要；warning 含"摘要压缩" | 2.5 |
| TC-T-11 | `test_preprocessor_falls_back_to_truncate_when_summary_rejects` | 分片远超可用输入预算 | 分片被截断（含警告文案）；warning 含"截断"；未发起摘要调用 | 2.5 兜底 |
| TC-T-12 | `test_chat_json_rejects_request_over_budget` | `MODEL_MAX_CONTEXT=100`，prompt 4000 字符 | 抛 `10003` 且**未发出真实请求**（`calls==0`） | 2.5 第3层 |
| TC-T-13 | `test_chat_json_truncates_when_enabled` | 同上但截断开启 | 实际发出的 user 消息被截断并带警告 | 2.5 |
| TC-T-14 | `test_chat_json_maps_context_overflow_to_10003` | 上游返回 maximum context length | 归一为 `10003`，且**不重试**（`calls==1`） | 2.3.2 兜底 |
| TC-T-15 | `test_chat_json_maps_other_errors_to_10004` | 上游 connection reset | 归一为 `10004`，重试 3 次（`max_retries=2`） | 2.5 |

### 4.4 单元测试 —— 入参校验与清洗（`test_validators.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-V-01 | `test_sanitize_instruction_blank_falls_back` | `""` / `None` / 空白串 | 均回落为"无特殊指令" | 2.4.2 |
| TC-V-02 | `test_sanitize_instruction_strips_control_chars` | 含 `\x00`、`\x1f` 的指令 | 控制字符被剥离，正文保留 | 2.4.2 |
| TC-V-03 | `test_sanitize_instruction_length_limit` | 恰好 1000 / 1001 字符 | 前者通过；后者抛 `10001`，提示含上限 | 2.1.2 / 2.7 |
| TC-V-04 | `test_parse_base_openapi_accepts_json_and_yaml` | `None`、空串、YAML、JSON、dict | 空值返回 `None`；其余正确解析 | 2.1.1 |
| TC-V-05 | `test_parse_base_openapi_rejects_invalid` | 非法 YAML / 数组 / `paths` 非对象（3 组参数化） | 均抛 `10001` | 2.1.2 |
| TC-V-06 | `test_enum_checks` | 合法与非法 `generate_mode` / `openapi_version` / `output_format` | 合法通过；非法抛 `10001` | 2.1.2 |
| TC-V-07 | `test_upload_count_and_size_limits` | 恰好/超出文件数上限；正常/超出文件大小 | 超限分别抛 `10001` / `10003` | 2.7 TC008/TC009 |
| TC-V-08 | `test_unique_dest_avoids_overwrite` | 同名文件连续 3 次；多段扩展名；空文件名 | 生成 `same.md` / `same_1.md` / `same_2.md`；`a.tar.gz`→`a.tar_1.gz`；空名回落 `upload.bin` | 2.3.1 |
| TC-V-09 | `test_unique_dest_strips_path_traversal` | 文件名 `../../etc/passwd` | 仅保留 basename `passwd` | 安全 |

### 4.5 集成测试 —— 工作流与降级（`test_workflow_flow.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-I-01 | `test_workflow_happy_path` | 1 段素材，抽取返回 1 个 POST 接口，风险扫描返回 1 条 | 产出目标接口；请求体字段类型正确；`diff is None`；LLM 扫描结果并入报告；system 含"永久固定规则"、user 含素材原文 | 2.3.1 |
| TC-I-02 | `test_multi_source_fusion_marks_conflict` | 两段素材对 `GET /pets` 参数不一致 | 合并为 1 个接口；`is_conflict=True`；参数取并集；仅 1 次融合调用且 prompt 含双方来源与用户指令；报告含该冲突 | 2.4.1 功能3 |
| TC-I-03 | `test_source_label_is_passed_to_extraction_prompt` | chunk 带 `source_type=sql` / `code` | 提取 prompt 含来源标签与文件名；未返回时回落为确定性标签 | 2.5 / 1.3 模块2 |
| TC-I-04 | `test_extraction_failure_skips_chunk` | 抽取抛 `10004`（2 个分片） | 不中断；`metas==[]`；`paths=={}`；不产生校验错误 | 降级路径1 |
| TC-I-05 | `test_fusion_failure_falls_back_to_first_source` | 两源冲突但融合调用失败 | 保留首份素材内容；强制 `is_conflict=True`；note 提示人工复核 | 降级路径2 |
| TC-I-06 | `test_risk_scan_failure_keeps_deterministic_report` | 风险扫描无应答 | 保留确定性规则结果（缺查询参数、全局缺响应结构） | 降级路径3 |

### 4.6 集成测试 —— 接口契约（`test_api_flow.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-I-07 | `test_generate_poll_result` | 提交文本素材 → 轮询 → 取结果 | 提交返回 200 + `task_id`(前缀 `task-`) + `status=pending` + `msg`；终态 `completed`；结果含 `paths`、参数、`risk_report`；`diff is None` | 2.1.1 / 2.3.1 |
| TC-I-08 | `test_generate_json_format_result` | `output_format=json` | `openapi` 可 `json.loads`，`responses.200.description=="OK"` | 2.1.1 |
| TC-I-09 | `test_cancel_running_task` | 慢响应模型（1.5s），提交后 0.3s 取消 | 取消接口 200；任务终态 `cancelled`；此时取结果返回 `10001`，错误体含 `msg` | 2.1.1 / 2.6.1 |

### 4.7 集成测试 —— 校验、模式、增量、状态（`test_api_validation.py`）

| 用例ID | 用例名称 | 输入 / 前置 | 预期结果 | 对应设计 |
|---|---|---|---|---|
| TC-I-10 | `test_invalid_enum_params_return_10001` | `output_format=xml`、`generate_mode=turbo`、`openapi_version=2.0`（3 组参数化） | 均 400 + `10001`，错误体含 `msg` | 2.1.2 |
| TC-I-11 | `test_too_long_user_instruction_returns_10001` | 指令长度 = 上限 + 1 | 400 + `10001`，detail 含上限值 | 2.7 TC007 |
| TC-I-12 | `test_too_many_files_returns_10001` | 上传 `MAX_UPLOAD_FILES + 1` 个文件 | 400 + `10001` | 2.7 TC008 |
| TC-I-13 | `test_oversized_file_returns_10003` | 单文件超出大小上限 | 422 + `10003` | 2.7 TC009 |
| TC-I-14 | `test_empty_input_returns_10001` | 不传文件也不传文本 | 400 + `10001` | 2.7 TC005 |
| TC-I-15 | `test_base_openapi_produces_diff` | 传基准契约 + 新素材 | 产物同时含 `/legacy`(保留) 与 `/pets`(新增)；`diff` 非空且 `added` 含新接口、`deleted` 为空 | 场景1 / TC003 |
| TC-I-16 | `test_invalid_base_openapi_returns_10001` | `base_openapi` 为非法 YAML | 400 + `10001` | 2.1.2 |
| TC-I-17 | `test_openapi_version_is_selectable` | `openapi_version=3.1.0` | 产物 `openapi` 字段为 `3.1.0` | 1.3 模块4 |
| TC-I-18 | `test_fast_mode_keeps_validation_errors_in_report` | 强制校验失败 + `generate_mode=fast` | 任务 `completed`；错误写入 `risk_report.validation_errors` | 降级路径4 |
| TC-I-19 | `test_strict_mode_fails_on_validation_error` | 强制校验失败 + `generate_mode=strict` | 任务 `failed`，`error_info.code==10005` | 2.2.2 |
| TC-I-20 | `test_progress_reported_on_completion` | 普通任务 | 终态 `completed` 且 `progress==100` | 2.2.2 |
| TC-I-21 | `test_unknown_task_returns_10002` | 查询不存在的 `task_id` | 404 + `10002` | 2.1.2 |

### 4.8 端到端冒烟（`scripts/smoke_http.py`，36 项检查）

真实 uvicorn 进程 + 真实 HTTP + 本地假 OpenAI 服务。

| 组 | 检查项 | 预期 |
|---|---|---|
| **[1] 服务存活** | `GET /openapi.json` | 200；挂载 4 个接口；`files` 字段标注 `binary` |
| **[2] 主链路** | 提交 → 轮询 → 取结果（13 项） | 200；`task_id` 格式正确；`status=pending`；有 `msg`；终态 `completed`；`progress==100`；`openapi` 为非空字符串；含 `paths`、`responses`+`schema`、`x-source`；`diff` 默认 `null`；`risk_report` 三件套齐全 |
| **[3] 增量** | 带 `base_openapi` 提交（5 项） | 提交 200；任务完成；`diff` 非空；`added` 含新接口；基准接口 `/legacy` 保留 |
| **[4] 文件上传** | 上传 `examples/sample_接口文档.md`（3 项） | 找到素材；提交 200；任务 `completed` |
| **[5] 取消** | 提交后立刻取消；取消未知任务（3 项） | 赶在结束前 → `cancelled`；已结束 → 404/`10002`；任务收敛终态；取消未知任务 404 |
| **[6] 错误码** | 7 类非法输入 + 错误体结构（8 项） | 未知任务 404/`10002`；空输入、非法 format/mode/version、指令过长、非法 `base_openapi` 均 400/`10001`；错误体严格为 `{code,msg,detail}` |
| **[7] 版本** | `openapi_version=3.1.0`（1 项） | 产物 `"openapi": "3.1.0"` |

### 4.9 设计文档测试用例映射（2.7 / 附录B）

| 设计用例 | 覆盖情况 | 对应实现用例 |
|---|---|---|
| TC001 基础提取 | ✅ | TC-U-05、TC-I-01、冒烟[2] |
| TC002 用户指令过滤接口 | ⚠️ 部分 | TC-I-02 仅验证指令进入 Prompt；**未端到端验证过滤效果**（见 G-02） |
| TC003 增量生成 | ✅ | TC-I-15、冒烟[3] |
| TC004 Token 超限 | ✅ | TC-T-07、TC-T-09、TC-T-12 |
| TC005 空输入 | ✅ | TC-I-14、冒烟[6] |
| TC006 无有效接口 | ❌ 未实现 | 见 G-03 |
| TC007 指令过长 | ✅ | TC-V-03、TC-I-11 |
| TC008 文件数超限 | ✅ | TC-V-07、TC-I-12 |
| TC009 文件过大 | ✅ | TC-V-07、TC-I-13 |
| TC010 strict 校验失败 | ✅ | TC-I-19 |
| TC011 fast 校验失败 | ✅ | TC-I-18 |
| TC012 抽取失败跳过 | ✅ | TC-I-04 |
| TC013 融合失败回退 | ✅ | TC-I-05 |
| TC014 风险扫描失败 | ✅ | TC-I-06 |
| TC015 取消任务 | ✅ | TC-I-09、冒烟[5] |

---

## 五、未覆盖与已知缺口

| 编号 | 缺口 | 影响 | 建议 |
|---|---|---|---|
| G-01 | **无准确率评测集**：设计 4.1 要求接口提取覆盖率 100%、准确率 ≥90%，当前零度量 | 无法证明"生成结果可信"，只能证明"流程不崩" | 建 ≥20 条真实素材 + 人工标注期望结果，加 pytest 参数化评测 |
| G-02 | **用户指令过滤无端到端验证**：只验证指令被送进 Prompt，未验证"忽略调试接口"确实过滤掉 | 若模型不遵守指令，无兜底也无检测 | 增加确定性后置过滤（按 path/tag 关键词）+ 对应用例 |
| G-03 | **TC006「无有效接口」未实现** | 该分支（`paths` 为空、提示未识别到接口）未被验证 | 补 1 条：素材无接口 → 任务成功、`paths=={}`、报告有提示 |
| G-04 | **进度中间态未验证**：只断言终态 `progress=100` | 10/20/40/60/75 的推进过程未被验证 | 用延迟假模型在中间态采样断言 |
| G-05 | **OCR / zip / 图片链路未测**：`paddleocr` 为可选依赖，`_parse_image` 与 zip 递归无单测 | 该链路回归无保护 | 装 OCR 依赖后补 zip（可离线）与图片用例 |
| G-06 | **性能未测**：设计 4.1 要求平均响应 ≤10s | 无基线，无法判断劣化 | 用假模型测调度开销；真实模型单独测端到端耗时 |
| G-07 | **并发未测**：多任务同时提交、`cancel` 与完成的竞态 | 竞态缺陷可能漏到线上 | 加并发提交 + 并发取消用例 |
| G-08 | **临时目录清理失败仅在沙箱可见**：清理失败会打 WARNING，但无用例断言 | 磁盘泄漏风险 | 注入不可删目录，断言日志与任务仍成功 |
| G-09 | **`res.json` / `openapi.yaml` 产物快照未纳入校验** | 产物与代码可能长期漂移（当前已漂移） | 加"快照与生成器一致"的回归用例 |

---

## 六、缺陷记录

| 编号 | 现象 | 根因 | 修复 | 回归用例 |
|---|---|---|---|---|
| BUG-01 | 上传文件必失败，报误导性的 `10003 素材解析错误` | 上传后直接写 `mkdtemp()` 新建目录，未考虑文件系统禁止写入运行时新建目录 | `api/service.py` 新增 `TempMaterials`：独立目录 → 写失败自动切换「已存在目录 + 唯一文件名前缀」扁平模式；写不进去时给出可操作提示 | TC-V-08、冒烟[4] |
| BUG-02 | 摘要分支永远不可达，超长分片一律被截断 | 触发阈值与摘要可行性检查共用同一预算（`per_chunk_token_limit`），互相卡死 | 拆分为「触发阈值」与「模型上下文预算」两个量（`_per_chunk_budget` / `_safe_input_budget`） | TC-T-10、TC-T-11 |
| BUG-03 | 确定性来源标签（curl/code/sql）从未进入提取 Prompt，模型自行猜来源 | `MetaExtractor._extract_chunk` 未把 chunk 的 `source_type` 传给模板 | 模板增加 `{{source_hint}}`，并在模型未返回时回落确定性标签 | TC-I-03 |

> 另有 2 次「测试环境事故」记录，避免误判为代码缺陷：
> ① 停服命令静默失败（`Get-Process | Where CommandLine` 在 Windows PowerShell 下属性不存在），旧进程占用端口导致新进程 bind 失败，冒烟全程打到旧代码；改用 `Get-NetTCPConnection -State Listen` 定位 `OwningProcess` 后解决。
> ② 清理后台任务时误杀假模型服务，导致一轮冒烟出现 3 个假失败；重启后 36/36 通过。

---

## 七、准入 / 准出标准

### 7.1 提测准入

- [x] 代码可导入：`python -c "import openapi_agent.main"`
- [x] 全量单测通过：`python -m pytest -q` → 63 passed
- [x] 依赖声明完整（`pyproject.toml`），无隐式依赖
- [x] 配置项全部可通过环境变量覆盖，无硬编码密钥

### 7.2 发布准出

- [x] 63 个单测 100% 通过
- [x] 36 项端到端冒烟 100% 通过
- [x] 五类错误码均有对应用例
- [x] 四条降级路径均有对应用例
- [x] 产物通过 OpenAPI 规范校验
- [ ] G-01 准确率评测达标（**未满足**，需评测集）
- [ ] G-06 性能基线（**未满足**，需真实模型环境）

> 结论：**流程正确性可发布（MVP 范围）**；准确率与性能指标待评测集与真实模型环境补齐后再做验收。
> 未打勾的两项不是本次改造的遗漏，而是需要外部资源（评测集标注人力、真实模型配额）才能开展的工作，已在第五节登记为 G-01 / G-06。

---

## 八、附录

### 8.1 错误码速查

| code | HTTP | 触发场景 | 相关用例 |
|---|---|---|---|
| `10001` | 400 | 空素材、枚举非法、指令过长、文件数超限、基线契约非法、任务未完成即取结果 | TC-V-01~07、TC-I-10~14、TC-I-16 |
| `10002` | 404 | `task_id` 不存在、任务失败后取结果、任务已终态不可取消 | TC-U-04、TC-I-09、TC-I-21 |
| `10003` | 422 | 无有效素材、单文件超限、素材总量超限、单分片超限、上下文超限、写入失败 | TC-T-03/07/09/12/14、TC-I-13 |
| `10004` | 503 | LLM 调用失败或超时（非上下文类） | TC-T-15 |
| `10005` | 500 | 缺模型配置、规范校验失败（strict）、未预期异常 | TC-I-19 |

### 8.2 新增用例规范

1. 命名：`test_<被测行为>_<预期>`，例如 `test_preprocessor_rejects_total_token_over_limit`；
2. 位置：按被测模块归入对应 `tests/test_*.py`，跨层链路放 `test_api_flow.py` / `test_api_validation.py`；
3. 断言：优先断言对外可观测结果（HTTP 状态、错误码、契约字段、报告结构）；
4. 模型：一律使用 `tests/fakes.py` 的 `FakeLlmClient`，禁止单测依赖真实模型；
5. 配置：用 `Settings` 注入（参考 `test_token_control.py::_settings`），不改全局环境变量；
6. 每条设计用例在第四节表格中登记 TC 编号与对应关系。
